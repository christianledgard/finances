{
  "name": "extractor",
  "version": "0.1.0",
  "private": true,
  "description": "EnableBanking (ING NL) extractor app (Python/FastAPI)",
  "scripts": {
    "install:py": "uv sync",
    "dev": "uv run uvicorn main:app --reload --port 8000",
    "start": "uv run uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
