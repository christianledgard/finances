{
  "name": "scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Daily cron scheduler that triggers the extractor /sync (Python)",
  "scripts": {
    "install:py": "pip install -e .",
    "start": "python main.py"
  }
}
