{
  "name": "@finances/db",
  "version": "0.1.0",
  "private": true,
  "description": "Shared MongoDB models and repository for the finances monorepo (Python)",
  "scripts": {
    "install:py": "pip install -e ."
  }
}
